CREATE TABLE STUDY_SITE_OWNER
(
    STUDY_SITE_ID BIGINT NOT NULL,
    USER_ID BIGINT NOT NULL,
    PRIMARY KEY (STUDY_SITE_ID, USER_ID)
);

ALTER TABLE STUDY_SITE_OWNER ADD CONSTRAINT FK_STUDY_SITE_OWNER_STUDY_SITE
    FOREIGN KEY (STUDY_SITE_ID) REFERENCES STUDY_SITE (IDENTIFIER)
    ON DELETE CASCADE;

ALTER TABLE STUDY_SITE_OWNER ADD CONSTRAINT FK_STUDY_SITE_OWNER_USER
    FOREIGN KEY (USER_ID) REFERENCES REGISTRY_USER (IDENTIFIER)
    ON DELETE CASCADE;