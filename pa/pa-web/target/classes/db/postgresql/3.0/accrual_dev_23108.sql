alter table submission add column create_user character varying(200);
alter table submission add column submit_user character varying(200);
alter table study_subject add column assigned_identifier character varying(200);
