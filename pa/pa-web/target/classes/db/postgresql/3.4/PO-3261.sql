CREATE TABLE SDC_DISEASE (
    IDENTIFIER SERIAL NOT NULL,
    DISEASE_CODE VARCHAR(10) NOT NULL CONSTRAINT sdc_disease_unique UNIQUE,
	CTEP_CATEGORY VARCHAR(200),
	CTEP_SUB_CATEGORY VARCHAR(200),
	CTEP_TERM VARCHAR(200),
	CTEP_SHORT_NAME VARCHAR(200),
    DATE_LAST_CREATED TIMESTAMP,
    USER_LAST_CREATED_ID INTEGER,
    DATE_LAST_UPDATED TIMESTAMP,
    USER_LAST_UPDATED_ID INTEGER,
    PRIMARY KEY (IDENTIFIER)
);

DELETE FROM performed_activity;

DELETE FROM study_subject;

ALTER TABLE study_subject DROP CONSTRAINT fk_study_subject_disease;

ALTER TABLE study_subject ADD CONSTRAINT fk_study_subject_disease 
    FOREIGN KEY (disease_identifier) REFERENCES sdc_disease (identifier) ON DELETE CASCADE;
